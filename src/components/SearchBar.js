import React from "react";

function SearchBar() {
    return (
        <div>
            <input className="border-primaryColor border-b-2 w-full" type="text" placeholder="Search Place"/>
        </div>
    );
}

export default SearchBar;